# This config defines mappings of project files to editor contexts
# For example contexts are taken into account by plugins choosing
# right language or snippet set.
# Here some examples. You MUST edit it to suite your needs.

# Map CHANGES file in project's root to be a 'rst' (restructured text) document.
/CHANGES: lang:rst

# Any glade file should be treated as 'xml' file.
*.glade: lang:xml

# Web project templates are 'html' files with additional 'mako' context,
# which allow to enable mako snippets support simultaneously with html snippets.
/tpl/*.html: ctx:mako

# Following match will ignore files generated by sphinx in quick open dialog
/doc/_build: quick_open:ignore 
